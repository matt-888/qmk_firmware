# RGB Matrix is enabled per keymap (the board defaults it off). OLED and pointing
# are driven by the holykeebs userspace via -e flags (e.g. -e OLED=yes), so build
# with USER_NAME=holykeebs.
RGB_MATRIX_ENABLE = yes

VIA_ENABLE = yes
